# Pinata JWT (create one at https://app.pinata.cloud/developers/api-keys)
# Needs pinFileToIPFS + pinJSONToIPFS scopes.
VITE_PINATA_JWT=

# Optional: override the public IPFS gateway used to preview uploads.
VITE_PINATA_GATEWAY=https://gateway.pinata.cloud
